// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: api/v1/analytics.proto

package arcov1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/loomi-labs/arco/backend/api/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// AnalyticsServiceName is the fully-qualified name of the AnalyticsService service.
	AnalyticsServiceName = "api.v1.AnalyticsService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// AnalyticsServiceIngestEventsProcedure is the fully-qualified name of the AnalyticsService's
	// IngestEvents RPC.
	AnalyticsServiceIngestEventsProcedure = "/api.v1.AnalyticsService/IngestEvents"
)

// AnalyticsServiceClient is a client for the api.v1.AnalyticsService service.
type AnalyticsServiceClient interface {
	// IngestEvents receives a batch of analytics events from the desktop client.
	//
	// Authentication:
	//
	//	Public endpoint. If auth context is present, the submitting user is
	//	associated automatically.
	//
	// Rate limits:
	//
	//	Subject to public API rate limiting (IP and global).
	//
	// Behavior:
	//
	//	Persists all events in the batch. Empty batches are accepted as no-ops.
	//	Events are idempotent — duplicate submissions are stored but can be
	//	deduplicated during aggregation.
	//
	// Client context:
	//
	//	Each event includes app version, OS, and locale for segmentation.
	IngestEvents(context.Context, *connect.Request[v1.IngestEventsRequest]) (*connect.Response[v1.IngestEventsResponse], error)
}

// NewAnalyticsServiceClient constructs a client for the api.v1.AnalyticsService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewAnalyticsServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) AnalyticsServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	analyticsServiceMethods := v1.File_api_v1_analytics_proto.Services().ByName("AnalyticsService").Methods()
	return &analyticsServiceClient{
		ingestEvents: connect.NewClient[v1.IngestEventsRequest, v1.IngestEventsResponse](
			httpClient,
			baseURL+AnalyticsServiceIngestEventsProcedure,
			connect.WithSchema(analyticsServiceMethods.ByName("IngestEvents")),
			connect.WithClientOptions(opts...),
		),
	}
}

// analyticsServiceClient implements AnalyticsServiceClient.
type analyticsServiceClient struct {
	ingestEvents *connect.Client[v1.IngestEventsRequest, v1.IngestEventsResponse]
}

// IngestEvents calls api.v1.AnalyticsService.IngestEvents.
func (c *analyticsServiceClient) IngestEvents(ctx context.Context, req *connect.Request[v1.IngestEventsRequest]) (*connect.Response[v1.IngestEventsResponse], error) {
	return c.ingestEvents.CallUnary(ctx, req)
}

// AnalyticsServiceHandler is an implementation of the api.v1.AnalyticsService service.
type AnalyticsServiceHandler interface {
	// IngestEvents receives a batch of analytics events from the desktop client.
	//
	// Authentication:
	//
	//	Public endpoint. If auth context is present, the submitting user is
	//	associated automatically.
	//
	// Rate limits:
	//
	//	Subject to public API rate limiting (IP and global).
	//
	// Behavior:
	//
	//	Persists all events in the batch. Empty batches are accepted as no-ops.
	//	Events are idempotent — duplicate submissions are stored but can be
	//	deduplicated during aggregation.
	//
	// Client context:
	//
	//	Each event includes app version, OS, and locale for segmentation.
	IngestEvents(context.Context, *connect.Request[v1.IngestEventsRequest]) (*connect.Response[v1.IngestEventsResponse], error)
}

// NewAnalyticsServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewAnalyticsServiceHandler(svc AnalyticsServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	analyticsServiceMethods := v1.File_api_v1_analytics_proto.Services().ByName("AnalyticsService").Methods()
	analyticsServiceIngestEventsHandler := connect.NewUnaryHandler(
		AnalyticsServiceIngestEventsProcedure,
		svc.IngestEvents,
		connect.WithSchema(analyticsServiceMethods.ByName("IngestEvents")),
		connect.WithHandlerOptions(opts...),
	)
	return "/api.v1.AnalyticsService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case AnalyticsServiceIngestEventsProcedure:
			analyticsServiceIngestEventsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedAnalyticsServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedAnalyticsServiceHandler struct{}

func (UnimplementedAnalyticsServiceHandler) IngestEvents(context.Context, *connect.Request[v1.IngestEventsRequest]) (*connect.Response[v1.IngestEventsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("api.v1.AnalyticsService.IngestEvents is not implemented"))
}
