{
  "mcpServers": {
    "linear": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote",
        "https://mcp.linear.app/mcp"
      ]
    },
    "sqlite": {
      "command": "uv",
      "args": [
        "--directory",
        "${ARCO_MCP_SQLITE_DIR}",
        "run",
        "mcp-server-sqlite",
        "--db-path",
        "${HOME}/.config/arco/arco.db"
      ]
    }
  }
}
